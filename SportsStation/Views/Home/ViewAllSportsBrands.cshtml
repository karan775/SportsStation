@model IEnumerable<SportsStation.Models.SportsBrand>
@{
    ViewData["Title"] = "Home Page";
}
    <div class="text-center">
        <h1 class="display-4" style="margin-bottom:50px;">All Sports Brands</h1>
    </div>

<div class="row" style="margin-bottom:50px;">
    @foreach (var item in Model)
    {
        <div class="col-md-3">
            <div class="card w-100">
                <center>
                    <img src="~/slide/SportsCart.png" alt="" style="width:100%">
                </center>
                <div class="card-body">
                    <h5 class="card-text">
                        @item.SportsBrandName
                    </h5>
                    <a class="btn btn-primary" asp-action="ViewSportsMaterialByBrand" asp-route-id="@item.SportsBrandID">View Product</a>
                </div>

            </div>
        </div>
    }
</div>